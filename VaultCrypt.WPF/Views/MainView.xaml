<UserControl x:Class="VaultCrypt.WPF.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="75" MinWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Choose action" HorizontalAlignment="Center" Grid.ColumnSpan="2"/>
        <Button Grid.Row="1" Grid.Column="0" Content="Create new vault" Command="{Binding CreateVaultCommand}"/>
        <Button Grid.Row="1" Grid.Column="1" Content="Open existing vault" Command="{Binding OpenVaultCommand}"/>
        <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="{Binding ApplicationVersion, FallbackValue=v0.0.0+1234567890123456789012345678901234567890}" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
