<UserControl x:Class="VaultCrypt.WPF.Views.ExceptionThrown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding ExceptionMessage,  FallbackValue=Exception Message}"/>
        <Button Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Content="OK" Command="{Binding OKCommand}"/>

    </Grid>
</UserControl>
