<UserControl x:Class="VaultCrypt.Views.EncryptionProgress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VaultCrypt.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <StackPanel Grid.Column="1" Orientation="Vertical">
            <TextBlock Text="Working..."/>
            <ProgressBar Grid.Row="1" Grid.Column="1" Minimum="0" Maximum="{Binding Context.Total}" Value="{Binding Context.Completed}"/>
            <TextBlock d:Text="0/0" HorizontalAlignment="Right">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0}/{1}">
                        <Binding Path="Context.Completed"/>
                        <Binding Path="Context.Total"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="OK" Command="{Binding FinishCommand}"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
