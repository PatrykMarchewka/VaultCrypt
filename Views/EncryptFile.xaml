<UserControl x:Class="VaultCrypt.Views.EncryptFile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VaultCrypt.Views"
             mc:Ignorable="d" 
             d:DesignHeight="52" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Encryption algorithm"/>
            <ComboBox ItemsSource="{Binding EncryptionAlgorithms}" SelectedItem="{Binding SelectedAlgorithm}"/>
            <TextBlock Text="Chunk size"/>
            <ComboBox ItemsSource="{Binding ChunkSizePresets}" SelectedItem="{Binding SelectedPreset}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Style="{StaticResource BlackTextStyle}" Text="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Encrypt" Command="{Binding EncryptCommand}"/>
            <Button Content="Cancel" Command="{Binding GoBackCommand}"/>
        </StackPanel>
        

    </Grid>
</UserControl>
