<UserControl x:Class="VaultCrypt.Views.PasswordInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VaultCrypt.Views"
             mc:Ignorable="d" 
             MinHeight="80" MinWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.ColumnSpan="2" Content=" Go back " HorizontalAlignment="Left" Command="{Binding GoBackCommand}"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Password" TextAlignment="Right"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Height="20" PasswordChar="#" PasswordChanged="PasswordBox_PasswordChanged"/>
        <Button Grid.Row="2" Grid.Column="1" Content=" Open " Command="{Binding OpenVaultCommand}"/>
    </Grid>
</UserControl>
