<UserControl x:Class="VaultCrypt.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VaultCrypt.Views"
             mc:Ignorable="d" 
             MinHeight="75" MinWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Choose action" HorizontalAlignment="Center" Grid.ColumnSpan="2"/>
        <Button Grid.Row="1" Content="Create new vault" Command="{Binding CreateVaultCommand}"/>
        <Button Grid.Row="1" Grid.Column="1" Content="Open existing vault" Command="{Binding OpenVaultCommand}"/>
    </Grid>
</UserControl>
